export default function ShopItem({children}: {children: React.ReactNode}){
    return ( 
        <div className = "grid grid-cols-[repeat(auto-fill,minmax(5rem,1fr))] justify-between py-4 gap-y-16 gap-16 flex-wrap">
        
        </div>
    )
}